{
  "name": "grand-rp-values",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_OPTIONS=--max_old_space_size=4096 next dev",
    "dev:clean": "rm -rf .next && cross-env NODE_OPTIONS=--max_old_space_size=4096 next dev",
    "dev:auto": "rm -rf .next && cross-env NODE_OPTIONS=--max_old_space_size=4096 next dev",
    "dev:deep": "rm -rf .next tsconfig.tsbuildinfo && cross-env NODE_OPTIONS=--max_old_space_size=4096 next dev",
    "dev:stable": "rm -rf .next && rm -rf node_modules/.cache && npm cache clean --force && cross-env NODE_OPTIONS=--max_old_space_size=4096 next dev",
    "dev:helper": "./scripts/dev-helper.sh",
    "build": "cross-env NODE_OPTIONS=--max_old_space_size=4096 next build",
    "build:clean": "rm -rf .next && cross-env NODE_OPTIONS=--max_old_space_size=4096 next build",
    "start": "cross-env NODE_OPTIONS=--max_old_space_size=4096 next start",
    "lint": "next lint",
    "clean": "rm -rf .next",
    "clean:all": "rm -rf .next node_modules package-lock.json && npm install"
  },
  "dependencies": {
    "@headlessui/react": "^2.2.7",
    "@heroicons/react": "^2.2.0",
    "@tailwindcss/line-clamp": "^0.4.4",
    "@vercel/analytics": "^1.5.0",
    "@vercel/speed-insights": "^1.2.0",
    "bcryptjs": "^3.0.2",
    "dotenv": "^17.2.1",
    "firebase-admin": "^13.4.0",
    "js-cookie": "^3.0.5",
    "lucide-react": "^0.539.0",
    "next": "15.4.6",
    "papaparse": "^5.5.3",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "tailwind-scrollbar": "^4.0.2",
    "tesseract.js": "^6.0.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.12",
    "@types/js-cookie": "^3.0.6",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "autoprefixer": "^10.4.21",
    "cross-env": "^7.0.3",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.12",
    "typescript": "^5"
  }
}
